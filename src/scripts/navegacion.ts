/*
  Navegacion del carrete: riel lateral, teclado, hint de scroll y escena activa.

  Implementacion completa en Fase 1 del ROADMAP. Responsabilidades previstas:

    - Generar el riel lateral (un boton por escena, etiqueta + punto).
    - IntersectionObserver (threshold ~.55) para marcar la escena activa.
    - Teclado: Up/Left/PageUp retrocede, Down/Right/PageDown avanza, Home/End extremos.
      Ignora si el foco esta en input/textarea/select/contenteditable o con meta/ctrl/alt.
    - Hint de scroll: aparece a ~1.3s, se oculta al primer scroll/tecla o a ~6.2s.
      Oculto en movil.
*/

export {};

// TODO(Fase 1): implementar generacion de riel, IntersectionObserver, teclado y hint.
