---
/*
  Ruta en ingles (`/en/`). Reutiliza la misma composicion que la raiz; Astro.currentLocale
  resuelve el idioma a 'en' por estar bajo /en/.
*/
import Pagina from '../../components/Pagina.astro';
---

<Pagina />
